services:
  powerwall_apps:
    restart: unless-stopped
    runtime: runsc-systrap
    cpus: 2
    mem_limit: 8g
    pids_limit: 2048
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
